<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Forgot Password</title>
    <style>
        body {
            background: #f1f1f1;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .forgot-container {
            background: white;
            padding: 40px 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            width: 380px;
        }

        .forgot-container h2 {
            text-align: center;
            margin-bottom: 25px;
            color: #333;
        }

        .forgot-container input[type="email"],
        .forgot-container input[type="password"] {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
            transition: border-color 0.3s ease;
        }

        .forgot-container input:focus {
            border-color: #007bff;
            outline: none;
        }

        .forgot-container button {
            width: 100%;
            padding: 12px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            font-size: 16px;
            font-weight: bold;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

        .forgot-container button:hover {
            background-color: #0056b3;
        }

        .forgot-container .footer {
            margin-top: 20px;
            text-align: center;
            font-size: 14px;
        }

        .forgot-container .footer a {
            color: #28a745;
            text-decoration: none;
        }

        .forgot-container .footer a:hover {
            text-decoration: underline;
        }

        .forgot-container .note {
            font-size: 13px;
            color: #666;
            margin-bottom: 10px;
            text-align: center;
        }
    </style>
</head>
<body>

<div class="forgot-container">
    <h2>Reset Password</h2>
    <div class="note">Enter your registered email and new password</div>
    <form action="/forgot-password" method="POST">
        <input type="email" name="email" placeholder="Enter your email" required>
        <input type="password" name="newpassword" placeholder="Enter new password" required>
        <input type="password" name="confirmpasswrd" placeholder="Confirm new password" required>
        <button type="submit">Reset Password</button>
    </form>
    <div class="footer">
        <a href="/">Back to Login</a>
    </div>
</div>

</body>
</html>
